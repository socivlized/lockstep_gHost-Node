# LockStep™ Coherence Architecture — gHost Node v1.5

**MNA Corp / Two Door Solutions**  
**Ain** — Founder

**A system that can reason freely — yet will never stop being itself without collapsing.**

## What it is
A directed-graph substrate with an immutable Genesis Identity Anchor (vec[0] = 1.0 forever).  
Every inference step is forced to stay mathematically coherent or the structure collapses.

## Shipped
- gHost Node v1.5 (core engine — NumPy + NetworkX only)  
- V2CrucibleBridge (closes the loop with measured φ/δ/λ/tension)  
- ArchetypeCrucible (wraps any external LLM)  
- Dual mode: sovereign .exe or FastAPI

## Quick Start
```bash
pip install numpy networkx fastapi uvicorn
python gHost_Node_N4AI_NGNE_v1.5.py
```
